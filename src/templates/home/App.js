import React from 'react';
import { Typography } from '@mui/material';

function Home() {
  return (
    <div style={{
      width: '100vw',
      height: '100vh',
    }}
    >
      <Typography variant="h2">Hello Word</Typography>
    </div>
  );
}

export default Home;
